#include<iostream>
int main()
{


std::cout<<"Welcome to C++(without escape sequence)"; //without escape sequence
std::cout<<"\nWelcome to C++(with escape sequence new line)"; //with escape sequence \n
std::cout<<"\tWelcome to C++(with escape sequence tab)"; //with escape sequence \t
std::cout<<"\nWelcome to C++\r(with escape sequence carriage return)"; //with escape sequence  \r
std::cout<<"\n\bWelcome  to C++(with escape sequence backspace)"; //with escape sequence  \b

    return 0;
}